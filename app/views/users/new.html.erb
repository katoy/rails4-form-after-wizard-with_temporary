<b><%= t(:new_user, scope: 'view') %></b>  ＞
<%= t(:about_you, scope: 'view') %>  ＞
<%= t(:where_can_we_find_you, scope: 'view') %>

<h1><%= t(:new_user, scope: 'view') %></h1>

<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>
      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label t(:email, scope: 'view') %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label t(:password, scope: 'view') %><br>
    <%= f.password_field :password %>
  </div>
  <div class="field">
    <%= f.label t(:password_confirmation, scope: 'view') %><br>
    <%= f.password_field :password_confirmation %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to t(:back, scope: 'view'), users_path %>
